import { Thing } from "openhab/types/things";
/**
 * Set all items to NULL for a given thing and its channels
 * @param thing
 */
export declare const clearAllLinkedThingItems: (thing: Thing) => void;
